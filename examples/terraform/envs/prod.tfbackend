# Backend values are injected by the pipeline via -backend-config flags
# (bucket/key/region/encrypt come from the stack parameters).
# This file is intentionally empty for the pipeline; fill it in for local runs:
#   bucket = "tf-artifacts-444455556666"
#   key    = "org-infra/state/prod/terraform.tfstate"
#   region = "us-east-1"
#   encrypt = true